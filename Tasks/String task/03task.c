#include<stdio.h>
#include<string.h>
void length(){
        char str[]="Have a nice day";
    int length=strlen(str);
    printf("Length of  string :%d",length);
}
int main(){
length();
    return 0;
}